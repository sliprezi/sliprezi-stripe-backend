{
  "name": "sliprezi-stripe-backend",
  "version": "1.0.0",
  "description": "SlipRezi backend for Stripe Checkout (manual capture)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "stripe": "^12.5.0"
  }
}
